import PublicationList from 'components/publication/PublicationList';

const PublicationListDetail = ({publications}) => (
    <PublicationList items={publications} />
);

export default PublicationListDetail;
